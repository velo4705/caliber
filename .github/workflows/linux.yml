name: Linux Build

on:
  workflow_dispatch:

# Linux builds are done locally:
#   bash package/build_linux.sh

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Linux builds are done locally."
          echo "  bash package/build_linux.sh"
          echo "Produces: .deb, .rpm, .tar.gz"
